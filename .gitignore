# Textbook PDFs are large (~170 MB); fetch them with `python fetch_sources.py`.
sources/*.pdf
# The cheat sheet is small and ships with the repo so it runs out of the box.
!sources/calculus_cheat_sheet_all.pdf

.chroma/
.venv/
venv/
__pycache__/
*.pyc
.env
.DS_Store
.vscode/
.idea/
